{{!-- The tag below includes the header - partials/header.hbs --}}
{{> header}}

{{!-- If we have a tag cover, display that - else blog cover - else nothing --}}
<header class="main-header post-head
    {{#if tag.cover}} grey colordodge" style="background-image: url({{url tag.cover}});
    {{else if @settings.cover}} brown softlight" style="background-image: url({{url @settings.cover}});
    {{else}}no-cover{{/if}}">
    <nav class="main-nav overlay clearfix">
        {{#if @settings.logo}}<a class="blog-logo" href="{{url}}"><img src="{{url @settings.logo}}" alt="{{@settings.title}}" /></a>{{/if}}
        <a class="menu-button icon-menu" href="#"><span class="word">Menu</span></a>
    </nav>
    <div class="vertical">
        {{#tag}}
        <div class="main-header-content inner">
            <h1 class="page-title">{{@settings.title}}</h1>
            <h2 class="page-description">{{name}}</h2>
        </div>
        {{/tag}}
    </div>
</header>

{{!-- The main content area on the homepage --}}
<main class="content" role="main">

    {{#tag}}
        {{#if meta_description}}<div class="meta-description inner">{{meta_description}}</div>{{/if}}
    {{/tag}}

    {{!-- The tag below includes the post loop - partials/loop.hbs --}}
    {{> loop}}

</main>

{{!-- The tag below includes the footer - partials/footer.hbs --}}
{{> footer}}